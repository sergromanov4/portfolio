import React from 'react';

import Logo from './components/Logo';

const Header: React.FC = () => {
    return (
        <div className="App">
            <Logo />
        </div>
    );
};

export default Header;
