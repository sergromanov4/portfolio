interface IMenuItem {
    name: string;
    link: string;
}

interface IContactsItem {
    name: string;
    link: string;
    icon: string;
}

interface ISkillsItem {
    name: string;
    icon: string;
}

export {
    IMenuItem,
    IContactsItem,
    ISkillsItem
};
